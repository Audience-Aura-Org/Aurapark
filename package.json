{
  "name": "aurapark-transport",
  "version": "1.0.0",
  "description": "Transport booking platform with Next.js and custom Node.js server",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "npm run build --workspace=@transport-platform/web && cp -r apps/web/.next .next && cp -r apps/web/public public",
    "build:web": "npm run build",
    "postbuild": "echo '✓ Build complete. Start with: npm start'",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "next": "16.1.6",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "workspaces": [
    "apps/*"
  ],
  "private": true
}